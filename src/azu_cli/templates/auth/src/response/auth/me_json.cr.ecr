module <%= project.camelcase %>::Auth
  struct MeResponse
    include Azu::Response::JSON

    getter user : Hash(String, JSON::Any)?

    def initialize(@user : Hash(String, JSON::Any)? = nil)
    end

    def to_json(io : IO)
      {user: @user}.to_json(io)
    end
  end
end


