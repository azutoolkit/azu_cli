module <%= @module_name %>
  struct <%= @name.camelcase %><%= @action.camelcase %>Endpoint
    include Azu::Endpoint(<%= @name.camelcase %><%= @action.camelcase %><%= @endpoint_type == "web" ? "Request" : "Request" %>, <%= @name.camelcase %><%= @action.camelcase %><%= @endpoint_type == "api" ? "Response" : "Page" %>)

    # TODO: Add HTTP verb and route for <%= @action %> action
    # Example: <%= http_verb_action %> "<%= action_path %>"

    def call : <%= @name.camelcase %><%= @action.camelcase %><%= @endpoint_type == "api" ? "Response" : "Page" %>
      # TODO: Implement <%= @action.downcase %> action logic
      <%= @name.camelcase %><%= @action.camelcase %><%= @endpoint_type == "api" ? "Response" : "Page" %>.new
    end
  end
end
